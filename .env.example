# Em desenvolvimento, deixe vazio para o proxy do Vite (/api → http://localhost:8080).
# Na Vercel, defina a origem pública da API (sem barra no final), por exemplo:
# https://api.vidaconecta.com.br
VITE_API_URL=
